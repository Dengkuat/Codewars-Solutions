const findInArray = (array, iterator) => {
  return array
}
console.log(findInArray())

//?input
//ok so we have 2 parameters right
//one is the array
//then then the second is the iterator right

//*info
//so with a function tht takes our 2 parameters
//a sequence so this has the type and length of items that are irrelvant 
//a function tha will be called on members of the sequece and their index, the function will return either true or false
//so the function must iterate though the members of the sequence in order utill the provided function returns truem at which 

//!test case